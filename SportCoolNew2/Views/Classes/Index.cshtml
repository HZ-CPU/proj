@using PagedList
@using PagedList.Mvc


@model IPagedList<SportCoolNew2.Models.Class>

@{
    ViewBag.Title = "影片";
}
<link href="~/Content/PagedList.css" rel="stylesheet" />

<div class="container " style="margin-top:1%;">

    @*影片分類*@

    @*大分類分三類 初中高階*@
    <nav>
        <div class="nav nav-tabs text-white  row" id="nav-tab" role="tablist">
            <a class="nav-link active text-center " style="width:33%;" id="nav-home-tab" data-toggle="tab" href="#nav-home" role="tab" aria-controls="nav-home" aria-selected="true">初階</a>
            <a class="nav-link text-center" style="width:33%;" id="nav-profile-tab" data-toggle="tab" href="#nav-profile" role="tab" aria-controls="nav-profile" aria-selected="false">進階</a>
            <a class="nav-link text-center" style="width:33%;" id="nav-contact-tab" data-toggle="tab" href="#nav-contact" role="tab" aria-controls="nav-contact" aria-selected="false">高階</a>
        </div>
    </nav>


    <div class="tab-content " id="nav-tabContent">
        @*初階*@
        <div class="tab-pane  active" id="nav-home" role="tabpanel" aria-labelledby="nav-home-tab">
            <ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
                <li class="nav-item" role="presentation">
                    <a class="nav-link active" id="pills-home-tab" data-toggle="pill" href="#pills-home" role="tab" aria-controls="pills-home" aria-selected="true">健身運動</a>
                </li>
                <li class="nav-item" role="presentation">
                    <a class="nav-link" id="pills-profile-tab" data-toggle="pill" href="#pills-profile" role="tab" aria-controls="pills-profile" aria-selected="false">飲食控制</a>
                </li>
                <li class="nav-item" role="presentation">
                    <a class="nav-link" id="pills-contact-tab" data-toggle="pill" href="#pills-contact" role="tab" aria-controls="pills-contact" aria-selected="false">戶外踏青</a>
                </li>
            </ul>
            <br />


            <div class="tab-content" id="pills-tabContent">
                @*1-1*@
                <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">


                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 1) && (@item.cTypeId == 1))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>


                </div>

                @*1-2*@
                <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab">

                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 1) && (@item.cTypeId == 2))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                </div>


                @*1-3*@
                <div class="tab-pane fade" id="pills-contact" role="tabpanel" aria-labelledby="pills-contact-tab">

                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 1) && (@item.cTypeId == 3))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                </div>
            </div>
        </div>




        @*進階*@
        <div class="tab-pane fade" id="nav-profile" role="tabpanel" aria-labelledby="nav-profile-tab">
            <ul class="nav nav-pills mb-3" id="pills-tab2" role="tablist">
                <li class="nav-item" role="presentation">
                    <a class="nav-link active" id="pills-home-tab2" data-toggle="pill" href="#pills-home2" role="tab" aria-controls="pills-home" aria-selected="true">健身運動</a>
                </li>
                <li class="nav-item" role="presentation">
                    <a class="nav-link" id="pills-profile-tab2" data-toggle="pill" href="#pills-profile2" role="tab" aria-controls="pills-profile" aria-selected="false">飲食控制</a>
                </li>
                <li class="nav-item" role="presentation">
                    <a class="nav-link" id="pills-contact-tab2" data-toggle="pill" href="#pills-contact2" role="tab" aria-controls="pills-contact" aria-selected="false">戶外踏青</a>
                </li>
            </ul>
            <div class="tab-content" id="pills-tabContent2">
                @*2-1*@
                <div class="tab-pane fade show active" id="pills-home2" role="tabpanel" aria-labelledby="pills-home-tab2">

                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 2) && (@item.cTypeId == 1))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                </div>

                @*2-2*@
                <div class="tab-pane fade" id="pills-profile2" role="tabpanel" aria-labelledby="pills-profile-tab2">

                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 2) && (@item.cTypeId == 2))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                </div>

                @*2-3*@
                <div class="tab-pane fade" id="pills-contact2" role="tabpanel" aria-labelledby="pills-contact-tab2">

                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 2) && (@item.cTypeId == 3))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                </div>

            </div>
        </div>

        @*高階*@
        <div class="tab-pane fade" id="nav-contact" role="tabpanel" aria-labelledby="nav-contact-tab">
            <ul class="nav nav-pills mb-3" id="pills-tab3" role="tablist">
                <li class="nav-item" role="presentation">
                    <a class="nav-link active" id="pills-home-tab3" data-toggle="pill" href="#pills-home3" role="tab" aria-controls="pills-home" aria-selected="true">健身運動</a>
                </li>
                <li class="nav-item" role="presentation">
                    <a class="nav-link" id="pills-profile-tab3" data-toggle="pill" href="#pills-profile3" role="tab" aria-controls="pills-profile" aria-selected="false">飲食控制</a>
                </li>
                <li class="nav-item" role="presentation">
                    <a class="nav-link" id="pills-contact-tab3" data-toggle="pill" href="#pills-contact3" role="tab" aria-controls="pills-contact" aria-selected="false">戶外踏青</a>
                </li>
            </ul>
            <div class="tab-content" id="pills-tabContent3">
                @*3-1*@
                <div class="tab-pane fade show active" id="pills-home3" role="tabpanel" aria-labelledby="pills-home-tab3">

                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 3) && (@item.cTypeId == 1))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                </div>

                @*3-2*@
                <div class="tab-pane fade" id="pills-profile3" role="tabpanel" aria-labelledby="pills-profile-tab3">

                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 3) && (@item.cTypeId == 2))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                </div>

                @*3-3*@
                <div class="tab-pane fade" id="pills-contact3" role="tabpanel" aria-labelledby="pills-contact-tab3">

                    <div class="row justify-content-start ">
                        @foreach (var item in Model)
                        {
                            if ((@item.cGradeId == 3) && (@item.cTypeId == 3))
                            {
                                <div class="col mb-6">
                                    <div class="card h-100 mt-6 border-0">
                                        @if (item.audio != null)
                                        {
                                            <a href="/Classes/Details/@item.cId">
                                                <video style="width:500px;" controls>
                                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                                </video>
                                            </a>
                                        }
                                        <div class="card-body">
                                            <h3 class="card-title">@item.className</h3>
                                            <p class="card-text">類別：@item.ClassType.typeName</p>
                                            <p class="card-text">項目：@item.Coach.expertise</p>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>


















      





























    @*<ul class="nav nav-tabs nav-fill">
        <li class="nav-item">
            <a class="nav-link active" href="#home" data-toggle="tab">初階</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#Item2" data-toggle="tab">進階</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#Item3" data-toggle="tab">高階</a>
        </li>
    </ul>

    <div class="tab-content">
        <div class="tab-pane show active fade w-100" id="home">
            <div class="row row-cols-1 row-cols-md-3">
                @foreach (var item in Model)
                {

                    if (item.audio == null)
                    {

                    }
                    else

                    {

                        <video style="width:100%" controls>
                            <source src="@Url.Content(item.audio)" type="video/mp4" />
                        </video>
                    }

                    <div class="card-body">
                        <h3 class="card-title">@item.className</h3>
                        <p class="card-text">類別：@item.ClassType1.typeName</p>
                        <p class="card-text">項目：@item.Coach.expertise</p>
                    </div>
                }

            </div>
        </div>
        <div class="tab-pane" id="Item2">
            <div class="row row-cols-1 row-cols-md-3">
                @foreach (var item in Model)
                {
                    <div class="col mb-4">
                        <div class="card h-100 mt-3">
                            @if (item.audio == null)
                            {
                                <h4>暫無圖示</h4>
                            }
                            else
                            {
                                <video style="width:100%" controls>
                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                </video>
                            }
                            <div class="card-body">
                                <h3 class="card-title">@item.className</h3>
                                <p class="card-text">類別：@item.ClassType1.typeName</p>
                                <p class="card-text">項目：@item.Coach.expertise</p>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
        <div class="tab-pane" id="Item3">
            <div class="row row-cols-1 row-cols-md-3">
                @foreach (var item in Model)
                {

                    <div class="col mb-4">
                        <div class="card h-100 mt-3">
                            @if (item.audio == null)
                            {
                                <h4>暫無圖示</h4>
                            }
                            else
                            {
                                <video style="width:100%" controls>
                                    <source src="@Url.Content(item.audio)" type="video/mp4" />
                                </video>
                            }
                            <div class="card-body">
                                <h3 class="card-title">@item.className</h3>
                                <p class="card-text">類別：@item.ClassType1.typeName</p>
                                <p class="card-text">項目：@item.Coach.expertise</p>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>

</div>
<div class="text-center">
    @Html.PagedListPager(Model, page => Url.Action("Index", new { page }),
    new PagedListRenderOptions
    {
        LinkToFirstPageFormat = "<<第一頁",
        LinkToPreviousPageFormat = "<前一頁",
        LinkToNextPageFormat = "後一頁>",
        LinkToLastPageFormat = "最末頁>>",
        PageCountAndCurrentLocationFormat = "第{0}頁/共{1}頁",
        DisplayPageCountAndCurrentLocation = true
    }
    )
</div>*@