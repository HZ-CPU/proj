@model SportCoolNew2.Models.Friend

@{
    ViewBag.Title = "Create";
}
    <div class="container">
        <h2>好友邀請</h2>


        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">
               
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                <div class="form-group">
                    @Html.LabelFor(model => model.fmId, "請輸入好友Id", htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">

                        @Html.EditorFor(model => model.fmId, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.fmId, "", new { @class = "text-danger" })
                        @*@if (ViewBag.nul3 != null)
                        {
                            if(Model.status == true)
                            {
                             <p class="text-danger">@ViewBag.nul3</p>
                            }
                            else
                            {
                               <p class="text-danger">@ViewBag.nul2</p>
                            }
                           
                        }
                        else*@
                       
                            <p class="text-danger">@ViewBag.nul</p>
                       
                    </div>
                </div>


                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="送出" class="btn btn-primary" />

                    </div>
                </div>
            </div>
        }

        <div>
            @Html.ActionLink("返回好友清單", "Index")
        </div>
    </div>
<br /><br /><br /><br /><br />
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
